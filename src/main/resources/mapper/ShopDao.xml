<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.znz.dao.ShopDao">

    <select id="queryShopById" resultType="Shop" parameterType="int">
        SELECT s.shop_id, s.name, s.avatar, s.description, s.owner_id, s.create_time,
        u.username as "owner.username"
        FROM shop s
        INNER JOIN user u ON s.owner_id = u.user_id
        WHERE shop_id = #{shopId}
    </select>

    <select id="queryShopsByParams" resultType="Shop">
        /*SELECT shop_id, name, avatar, description, owner_id, create_time
        FROM shop
        order by create_time DESC*/

        SELECT s.shop_id, s.name, s.avatar, s.description, s.owner_id, s.create_time,
        u.username as "owner.username"
        FROM shop s
        INNER JOIN user u ON s.owner_id = u.user_id
    </select>
</mapper>
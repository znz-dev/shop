<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.znz.dao.UserDao">

    <select id="queryUserById" resultType="User" parameterType="int">
        SELECT user_id, username, password, phone, email, real_name, avatar, description, create_time
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="queryUsersByParams" resultType="User">
        SELECT user_id, username, password, phone, email, real_name, avatar, description, create_time
        FROM user
        order by create_time DESC
        limit #{offset}, #{limit}
    </select>

    <select id="queryUsersCountByParams" resultType="int">
        SELECT COUNT(*)
        FROM user
    </select>
</mapper>
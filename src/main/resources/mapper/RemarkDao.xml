<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.znz.dao.RemarkDao">

    <insert id="createRemarkByParams">
        INSERT INTO remark
        (remark_id, content, score, good_id, order_id)
        VALUES
        (#{remarkId},
        #{content},
        #{score},
        #{goodId},
        #{orderId}
        )
    </insert>

    <select id="queryRemarksByGoodId" resultType="Remark">
        SELECT
            remark_id, content, score, create_time, good_id, order_id
        FROM
            remark
        WHERE
            good_id = #{goodId}
        order by create_time DESC
        limit #{offset}, #{limit}
    </select>

    <select id="queryRemarkCountByGoodId" resultType="int">
        SELECT
            COUNT(*)
        FROM
            remark
        WHERE
            good_id = #{goodId}
    </select>

</mapper>
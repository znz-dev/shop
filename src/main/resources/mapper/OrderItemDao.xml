<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.znz.dao.OrderItemDao">

    <insert id="createOrderItemByParams">
        INSERT INTO order_item
        (item_id, order_id, good_id, price, numeber, option_id)
        VALUES
        (
        #{itemId},
        #{orderId},
        #{goodId},
        #{price},
        #{number},
        #{optionId}
        )
    </insert>

    <select id="queryOrderItemsByOrderId" resultType="OrderItem">
        SELECT
            item_id, order_id, good_id, price, numeber, option_id
        FROM
            order_item
        WHERE
            order_id = #{orderId}
    </select>

</mapper>